<UserControl x:Class="NeptunoApp.Views.ReportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Margin="24,20">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,14">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Reporte de detalles de pedidos" FontSize="22" FontWeight="Bold"/>
                <TextBlock Text="Consulta mediante INNER JOIN y rango de fechas" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,2,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Date Range Filter Card -->
        <Border Grid.Row="1" Background="{StaticResource CardBgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Fecha Inicial *" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding FechaInicio}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Fecha Final *" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding FechaFin}"/>
                </StackPanel>

                <Button Grid.Column="4" Style="{StaticResource OrangeButton}" Content="📊 Consultar Reporte" Command="{Binding ConsultarCommand}" VerticalAlignment="Bottom" Padding="22,8"/>

                <TextBlock Grid.Row="1" Grid.ColumnSpan="5" Text="{Binding Mensaje}" Foreground="#0F766E" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,10,0,0"/>
            </Grid>
        </Border>

        <!-- Grid Results Card -->
        <Border Grid.Row="2" Background="{StaticResource CardBgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ProgressBar Grid.Row="0" IsIndeterminate="True" Height="4" Margin="0,0,0,8" Visibility="{Binding EstaCargando, Converter={StaticResource BoolToVis}}"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Lineas}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="N° Pedido" Binding="{Binding PedidoID}" Width="75"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FechaPedido, StringFormat='{}{0:dd/MM/yyyy}'}" Width="95"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding NombreCliente}" Width="1.5*"/>
                        <DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" Width="1.5*"/>
                        <DataGridTextColumn Header="Precio Unit." Binding="{Binding PrecioUnidad, StringFormat='S/ {0:N2}'}" Width="95"/>
                        <DataGridTextColumn Header="Cant." Binding="{Binding Cantidad}" Width="60"/>
                        <DataGridTextColumn Header="Dscto." Binding="{Binding Descuento, StringFormat='{}{0:P0}'}" Width="65"/>
                        <DataGridTextColumn Header="Subtotal (S/)" Binding="{Binding Subtotal, StringFormat='S/ {0:N2}'}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Total Footer -->
                <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16,10" Margin="0,12,0,0">
                    <TextBlock Text="{Binding Total, StringFormat='Total acumulado en el período: S/ {0:N2}'}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Right"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
